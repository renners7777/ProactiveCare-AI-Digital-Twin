{
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "yarn workspace web dev",
    "mobile": "yarn workspace mobile start",
    "test": "yarn workspaces run test"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}